# ATLAS MIND PWA ORGANISM V1 — 2026-09-13

Campaign: `GA-ATLAS-MIND-PWA-ORGANISM-V1-2026-09-13`
Status: ACTIVE / CONVERGENCE ONLY
Created: 2026-09-13
Canonical owner: `Atlas-Ascend/Atlas-Mind-LLM`
Production web source: `Atlas-Ascend/nextjs-ai-chatbot`

## Purpose

This folder is the dated/versioned locator for the Atlas Mind operator MVP. It does not create a new Atlas Mind, runtime, model fabric, executive layer, memory system, or product lineage.

The existing Atlas Mind identity is exposed through one installable web surface while preserving the existing governed organism:

`Atlas Mind PWA -> Vercel -> GA-NIF/Render -> JANUS -> Packet OS / Workforce -> capability target (including EDEN where selected) -> ProofGrid -> THOTH -> Neon -> Atlas Mind`.

## Lane 1 — installable organism/operator surface

Canonical target:

1. repair the incomplete PWA implementation already referenced by `nextjs-ai-chatbot/app/layout.tsx`;
2. add a consumer-safe `/organism` page inside `atlasmind.global` without exposing private operator authority;
3. preserve `/atlas` as the governed organism/operator surface;
4. preserve `runAtlasCloudTurn()` as the single normal-path cognition/action controller;
5. make mobile/browser installation the MVP embodiment instead of blocking on native Android packaging;
6. bind Termux later as another client of the same Atlas identity, not a second intelligence;
7. bind physical EDEN as a governed execution/capability target, not a competing Atlas Mind.

## Lane 2 — AtlasMind.Global responsibility closure

Forensically audit the existing consumer portal, repair concrete defects, enumerate unfinished responsibilities, assign canonical owners, and require proof for every promoted domain. A rendered page is not completion proof.

The web-source forensic queue and receipts live under the same dated path in `Atlas-Ascend/nextjs-ai-chatbot`.

## Frozen boundaries

- Consumer traffic must not become a backdoor into private estate execution.
- Conversation and state-changing execution remain separated by routing/JANUS authority even when the user experiences one natural-language input surface.
- Render remains persistent runtime/execution; Vercel remains Atlas Mind web/edge surface; Neon remains durable state; GitHub remains source/Build Truth; Base44 remains rapid operational-app surface.
- Existing Wave 00 persistence/restart/recall acceptance remains authoritative and may not be bypassed by a PWA install receipt.
- Native Ark-OS Android embodiment is preserved, not deleted; it is not an MVP blocker.

## Definition of MVP proof

MVP is promoted only after all of the following have direct evidence:

- PWA manifest and service worker are served from production;
- installable standalone launch succeeds on a real phone/browser;
- `/try` is a live onboarding doorway rather than a dead route;
- `/organism` is reachable from the consumer portal and maintains public/private boundaries;
- authenticated Atlas Mind returns genuine model output;
- a bounded natural-language action reaches JANUS and completes with matching packet, ProofGrid, and THOTH evidence;
- the same durable conversation/evidence survives restart/re-authentication according to Wave 00;
- EDEN execution is not claimed until an actual EDEN-targeted run is receipted.
