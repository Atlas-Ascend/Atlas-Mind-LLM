# 10 — State, Memory, and Provenance

Context separates conversation state, retrieved memory, authoritative MAAT state, model inference, and tool results. Claims carry provenance class where material.

Long-term memory writes are candidates subject to Thoth policy; conversation text is not automatically canonical memory.